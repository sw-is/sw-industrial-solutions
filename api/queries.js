import { gql} from '@apollo/client';export const GLOBALS = gql`    fragment Globals on Entry {        Globals{            items {                mainAddress                mainPhoneNumber                mainEmail		            ustId		            menuAbout		            menuServices		            menuContact		            btnContactUs		            btnSubmit		            formTypeYourEmail		            formMessagePlaceholder            }        }    }`;export const HERO_SECTION = gql`    fragment TopIntroduction on Entry {        TopIntroduction{            items {                companyName                mainHeader                descriptionLine1                descriptionLine2                scrollDown            }        }    }`;export const GET_CONTENT = gql`    query GetContent($locale: String, $limit: Int = 1) {        globalsCollection(locale:$locale, limit: $limit) {            items {                mainAddress                mainPhoneNumber                mainEmail                ustId                menuAbout                menuServices                menuContact                btnContactUs                btnSubmit                formTypeYourEmail                formMessagePlaceholder            }        }        topIntroductionCollection(locale:$locale, limit: $limit) {            items {                companyName                mainHeader                descriptionLine1                descriptionLine2                scrollDown            }        }    }`;